import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function Taxt() {
  return (
    <Text style={{
        fontFamily: 'Montserrat-Regular',
        color: 'red'
      }}>Font Checking!</Text>
  )
}

const styles = StyleSheet.create({})